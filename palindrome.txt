declare
n number:=&n;
rev number:=0;
rem number;
temp number:=n;
begin
while n>0 loop
rem:=mod(n,10);
rev:=(rev*10)+rem;
n:=trunc(n/10);
end loop;
if temp=rev then
dbms_output.put_line(temp||'is palindrome');
else
dbms_output.put_line(temp||'is not');
end if;
end;
